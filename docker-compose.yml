services:
  backend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: rentwheels-backend
    env_file:
      - ./.env
    volumes:
      - ./.env:/app/.env:ro
    ports:
      - "8000:8000"
    restart: unless-stopped
